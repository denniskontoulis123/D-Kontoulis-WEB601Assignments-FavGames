import { GameDirectiveDirective } from './game-directive.directive';

describe('GameDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new GameDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
